// import axios from 'axios';

describe('GET /api/v1/properties', () => {
  test.todo('should return all properties from the current user');
});

describe('GET /api/v1/properties/<id>', () => {
  test.todo('should return details from a single property of the current user');
});

describe('POST /api/v1/properties', () => {
  test.todo('should create a new property for the authenticated user');
});

describe('PUT /api/v1/properties/<id>', () => {
  test.todo('should update one existing property for the authenticated user');
});

describe('DELETE /api/v1/properties/<id>', () => {
  test.todo('should delete one existing property for the authenticated user');
});
